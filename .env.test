SESSION_DRIVER=memory

# Level Bot (test: mocked provider, never called in tests)
LEVEL_BOT_URL=http://level-bot.test
LEVEL_BOT_API_KEY=test-key

# Mail (test: never sent)
MAIL_MAILER=smtp
MAIL_FROM_NAME=Pdnode Elections
MAIL_FROM_ADDRESS=admin@system.pdnode.com
SMTP_HOST=mail.pdnode.com
SMTP_PORT=465
SMTP_USERNAME=admin@system.pdnode.com
SMTP_PASSWORD=test-only
