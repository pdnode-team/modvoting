# pnpm 11 settings（pnpm 11.17 实测：allowBuilds 须为对象格式 {pkg: true}；
# 数组格式（onlyBuiltDependencies）在此版本不生效，会 ERR_PNPM_IGNORED_BUILDS 失败）
allowBuilds:
  '@swc/core': true
  better-sqlite3: true
  esbuild: true
  fsevents: true

overrides:
  eslint-plugin-react>eslint: $eslint
